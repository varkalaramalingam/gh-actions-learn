name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello World!"
      - name: multiline commands
        run: |
          echo "Name: Ramalingam"
          echo "LastName: Varkala"
      - name: Print Goodbye
        run: echo "Done - bye!"
      
